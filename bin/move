#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board =[" "," "," "," "," "," "," "," "," "]

puts "Welcome to Tic Tac Toe!"


index = gets.strip

input_to_index(index, board, "X")

input_to_index(index, board, "O")

input_to_index(index, board, "X")

input_to_index(index, board, "O")

input_to_index(index, board, "X")

input_to_index(index, board, "O")

input_to_index(index, board, "X")

input_to_index(index, board, "O")

input_to_index(index, board, "X")
